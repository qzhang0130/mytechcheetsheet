add_subdirectory()   # add external or 3rd party subidrs with external projects' source code
git submodules # doing exactly the same as add_subdirectory()
find_package()
-DCMAKE_BUILD_TYPE=Release
add_library()
add_executable()
target_link_libraries()
